# @package _global_

output_dir: outputs/${accelerator.project_name}/clip

dataset:
  batch_size: 4

accelerator:
  # mixed_precision: BF16
  mixed_precision: "fp16" # for debug accelerator type
  project_name: reward_final
